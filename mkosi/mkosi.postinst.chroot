#!/bin/bash
set -e
set -o pipefail

[ "$1" = "final" ] || exit 0

if ! [ -d /root/installation ]; then
    git clone https://github.com/ASPP/installation /root/installation
fi

ansible-playbook -i localhost, --connection local /root/installation/all.yml
