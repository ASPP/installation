---
- name: Reload sshd config
  ansible.builtin.systemd_service:
    name: sshd.service
    state: reloaded

- name: Set default plymouth theme and force initrd rebuild
  ansible.builtin.command: 'plymouth-set-default-theme -R bgrt'
  tags: config
